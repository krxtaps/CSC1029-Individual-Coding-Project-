package Assessment2.part01;

import java.util.*;
import java.awt.Color;

public class ImageTest {
    /*
    // creating a constant value called max
// create a constant value called MAX
    // Define a constant PROMPT
    static final String PROMPT = "---->";
    // Define a constant SPACES
    static final String SPACES = "     ";
    // String array of menu options
    static final String options[] = {"Add Image", "Search", "Display All", "Exit"};
    // instance QUIT
    static final int QUIT = options.length;
    // defining count for objects in array
    static int count = 0;
    // menu title
    static String title = "Image Manager";
    // manu with title & options
    static ImageManager myMenu = new ImageManager(title, options);
    // scanner
    static Scanner input = new Scanner(System.in);

    public static void main(String[] args) {
        int choice;
        do {
            myMenu.display();
            choice = myMenu.getChoice();
            if (choice != QUIT) {
                processChoice(choice);
            }
        }
        while (choice != QUIT); // or while (quit == false)
        System.out.println("\nGoodbye!");
    }

    private static void processChoice(int choice) {
        boolean running = true;
        while (running) {
            switch (choice) {
                case 1:
                    addImage();
                    break;
                case 2:
                    searchId();
                    break;
                case 3:
                    listAllImages();
                    break;
                case 4:
                    System.out.println(QUIT);
                default:
                    System.out.println("Option " + choice + " is invalid.");
            }
            System.out.println();
        }
    }*/
}
/*        private static String[] getTitle() {
            String[] res = null;
            if (count > 0) {
                res = new String[count];
                for (int index = 0; index < count; index++) {
                    res[index] = pets[index].getTitle();
                }
            }
            return res;
        }
        */
